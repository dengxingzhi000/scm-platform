# 技术风险评估报告

> **项目**: SCM Platform 供应链管理系统
> **版本**: v1.0
> **评估日期**: 2025-12-24
> **评估人**: 架构组 + PMO
> **参考标准**: PMI PMBOK 风险管理 + 阿里技术风险管理体系

---

## 执行摘要

本报告识别了 SCM Platform 项目的 **18 项关键技术风险**，按优先级分为：
- **高风险 (P1)**: 5 项 - 需要立即制定应对措施
- **中风险 (P2)**: 8 项 - 需要持续监控
- **低风险 (P3)**: 5 项 - 知晓即可

**风险热图**:
```
影响程度
  高 ┃  R7   R1   R2
  中 ┃  R10  R3   R4
  低 ┃  R15  R12  R8
     ┗━━━━━━━━━━━━━━━━
      低   中   高
          发生概率
```

---

## 风险评估矩阵

| ID | 风险描述 | 概率 | 影响 | 优先级 | 风险值 | 责任人 |
|----|---------|------|------|--------|--------|--------|
| R1 | Seata 分布式事务性能瓶颈 | 高 | 高 | P1 | 9 | 架构师 |
| R2 | Elasticsearch 集群稳定性问题 | 高 | 高 | P1 | 9 | 运维组 |
| R3 | Redis 缓存穿透/雪崩 | 中 | 高 | P1 | 6 | 后端开发 |
| R4 | Canal 数据同步延迟过大 | 中 | 中 | P2 | 4 | 后端开发 |
| R5 | XXL-Job 单点故障 | 低 | 高 | P2 | 3 | 运维组 |
| R6 | 数据库分片键选择不当 | 中 | 中 | P2 | 4 | 架构师 |
| R7 | 团队对新技术栈不熟悉 | 高 | 中 | P2 | 6 | PM |
| R8 | 秒杀场景超卖问题 | 低 | 高 | P2 | 3 | 后端开发 |
| R9 | 第三方物流 API 不稳定 | 中 | 中 | P2 | 4 | 后端开发 |
| R10 | Nacos 配置中心故障 | 中 | 高 | P1 | 6 | 运维组 |
| R11 | 微服务链路调用超时 | 中 | 中 | P2 | 4 | 后端开发 |
| R12 | 数据库连接池耗尽 | 低 | 中 | P3 | 2 | 后端开发 |
| R13 | JVM 内存溢出 | 低 | 中 | P3 | 2 | 后端开发 |
| R14 | 网络带宽不足 | 低 | 低 | P3 | 1 | 运维组 |
| R15 | 日志数据量过大 | 中 | 低 | P3 | 2 | 运维组 |
| R16 | Spring Boot 4 兼容性问题 | 低 | 中 | P3 | 2 | 后端开发 |
| R17 | 测试环境资源不足 | 中 | 低 | P3 | 2 | 运维组 |
| R18 | 代码质量下降 | 低 | 低 | P3 | 1 | Tech Lead |

**风险值计算**: 风险值 = 概率 × 影响（高=3，中=2，低=1）

---

## 高优先级风险 (P1) 详细分析

### R1: Seata 分布式事务性能瓶颈 ⚠️

**风险描述**:
Seata AT 模式在高并发场景下（TPS > 5000）可能成为系统瓶颈，导致订单创建响应时间过长。

**发生概率**: 高 (70%)

**影响分析**:
- **业务影响**: 订单创建延迟增加，用户体验下降
- **性能影响**: Seata Server CPU 使用率 > 80%，事务协调延迟 > 100ms
- **财务影响**: 大促期间订单创建失败，预计损失 5%+ 订单

**根因分析**:
1. Seata Server 单节点性能上限约 5000 TPS
2. AT 模式需要两阶段提交，网络往返次数多
3. 全局锁竞争（同一行数据并发更新）

**应对措施**:

**缓解策略 (Mitigation)**:
1. **Seata Server 集群化**:
   ```yaml
   # 部署 3 节点 Seata 集群
   seata-server-1: 负载 33%
   seata-server-2: 负载 33%
   seata-server-3: 负载 34%
   ```

2. **TCC 模式改造高并发场景**:
   - 秒杀场景使用 TCC 模式
   - 库存预占改为 TCC 实现
   - 预计性能提升 50%

3. **分支事务合并**:
   - 减少不必要的远程调用
   - 批量操作合并为单个分支事务

**应急预案 (Contingency)**:
- 如果 Seata 宕机，订单服务降级为本地事务 + 异步补偿
- 定期演练 Seata 故障切换流程

**监控指标**:
- Seata Server CPU 使用率 > 70% → 告警
- 全局事务平均耗时 > 500ms → 告警
- 事务失败率 > 1% → 告警

**责任人**: 架构师（张三）
**截止日期**: Phase 1 结束前
**当前状态**: 🟡 进行中

---

### R2: Elasticsearch 集群稳定性问题 ⚠️

**风险描述**:
Elasticsearch 集群在高 QPS 场景下可能出现节点宕机、索引损坏、查询超时等问题。

**发生概率**: 高 (60%)

**影响分析**:
- **业务影响**: 商品搜索不可用，用户无法搜索商品
- **性能影响**: 搜索响应时间 > 1s，p99 延迟 > 5s
- **财务影响**: 搜索不可用 1 小时，预计损失订单 10%

**根因分析**:
1. ES 堆内存配置不当，触发 GC
2. 索引分片设计不合理，单分片数据量过大
3. 查询语句未优化，深度分页查询慢

**应对措施**:

**预防策略 (Prevention)**:
1. **ES 集群规划**:
   ```yaml
   # 3 节点集群
   - 1 master node (4C 8G)
   - 2 data node (8C 16G, 500G SSD)
   - 堆内存设置为物理内存的 50% (最大 31G)
   ```

2. **索引优化**:
   - 商品索引设置 3 分片 2 副本
   - 单分片数据量控制在 30GB 以内
   - 定期 force merge 减少段数量

3. **查询优化**:
   - 禁用深度分页（max_result_window=10000）
   - 使用 scroll API 或 search_after 分页
   - 慢查询日志监控（> 200ms 记录）

**监控指标**:
- ES 堆内存使用率 > 85% → 告警
- GC 时间占比 > 10% → 告警
- 查询 P99 延迟 > 500ms → 告警
- 集群状态非 green → 立即告警

**应急预案**:
- ES 宕机时降级到 MySQL 模糊查询（性能差但可用）
- 准备 ES 快照备份，故障时快速恢复

**责任人**: 运维组（李四）
**截止日期**: Phase 2 结束前
**当前状态**: 🟡 进行中

---

### R3: Redis 缓存穿透/击穿/雪崩 ⚠️

**风险描述**:
Redis 缓存失效时，大量请求直接打到数据库，导致数据库压力过大甚至宕机。

**发生概率**: 中 (50%)

**影响分析**:
- **业务影响**: 系统响应变慢，部分接口超时
- **性能影响**: 数据库 QPS 暴增 10 倍，CPU 使用率 > 90%
- **财务影响**: 系统不可用 30 分钟，预计损失 5% 订单

**三种缓存问题**:

1. **缓存穿透**: 查询不存在的数据，缓存和数据库都没有
2. **缓存击穿**: 热点数据过期，大量并发请求同时查询数据库
3. **缓存雪崩**: 大量缓存同时过期，数据库压力暴增

**应对措施**:

**缓存穿透解决方案**:
```java
// 1. 布隆过滤器
@Bean
public BloomFilter<String> productBloomFilter() {
    BloomFilter<String> filter = BloomFilter.create(
        Funnels.stringFunnel(Charset.defaultCharset()),
        1000000, // 预计 100 万商品
        0.01     // 误判率 1%
    );
    // 将所有商品 ID 加入布隆过滤器
    productMapper.selectAllIds().forEach(filter::put);
    return filter;
}

// 2. 空值缓存
public Product getProduct(UUID id) {
    String key = "product:" + id;
    Product product = redis.get(key);

    if (product == null) {
        product = db.query(id);
        if (product == null) {
            // 缓存空值，TTL 设置短一些
            redis.set(key, NULL_VALUE, 5, TimeUnit.MINUTES);
            return null;
        }
        redis.set(key, product, 10, TimeUnit.MINUTES);
    }

    return product == NULL_VALUE ? null : product;
}
```

**缓存击穿解决方案**:
```java
// 分布式锁 + 双重检查
public Product getProduct(UUID id) {
    String key = "product:" + id;
    Product product = redis.get(key);

    if (product == null) {
        String lockKey = "lock:product:" + id;

        try (LockHandle lock = distributedLock.acquire(lockKey, Duration.ofSeconds(10))) {
            // 双重检查
            product = redis.get(key);
            if (product == null) {
                product = db.query(id);
                redis.set(key, product, 10, TimeUnit.MINUTES);
            }
        }
    }

    return product;
}
```

**缓存雪崩解决方案**:
```java
// 1. 过期时间加随机值
int ttl = 600 + ThreadLocalRandom.current().nextInt(60); // 10分钟 ± 1分钟
redis.set(key, value, ttl, TimeUnit.SECONDS);

// 2. 缓存永不过期 + 后台异步更新
@Scheduled(fixedRate = 600000) // 每10分钟
public void refreshCache() {
    List<Product> hotProducts = getHotProducts();
    hotProducts.forEach(product -> {
        redis.set("product:" + product.getId(), product); // 不设置过期时间
    });
}
```

**监控指标**:
- Redis 缓存命中率 < 90% → 告警
- 数据库 QPS 突增 > 500% → 告警
- 布隆过滤器误判率 > 2% → 告警

**责任人**: 后端开发（王五）
**截止日期**: Phase 3 结束前
**当前状态**: 🟡 进行中

---

### R10: Nacos 配置中心故障 ⚠️

**风险描述**:
Nacos 作为配置中心和服务注册中心，如果宕机会导致所有微服务无法启动或发现服务。

**发生概率**: 中 (40%)

**影响分析**:
- **业务影响**: 所有微服务不可用，系统完全瘫痪
- **财务影响**: 系统宕机 1 小时，预计损失 100% 订单

**应对措施**:

**高可用方案**:
```yaml
# Nacos 集群部署（3 节点）
nacos-1: 192.168.1.101:8848
nacos-2: 192.168.1.102:8848
nacos-3: 192.168.1.103:8848

# 客户端配置多个 Nacos 地址
spring:
  cloud:
    nacos:
      server-addr: 192.168.1.101:8848,192.168.1.102:8848,192.168.1.103:8848
```

**故障演练**:
- 每月进行 Nacos 故障演练
- 模拟单节点宕机、网络分区等场景

**监控指标**:
- Nacos 节点健康检查失败 → 立即告警
- 服务注册/发现失败率 > 1% → 告警

**责任人**: 运维组（赵六）
**截止日期**: Phase 0 结束前
**当前状态**: 🔴 待开始

---

## 中优先级风险 (P2)

### R4: Canal 数据同步延迟过大

**风险描述**: Canal 同步 MySQL binlog 到 Elasticsearch 延迟 > 5 秒

**应对措施**:
- Canal 部署主备模式
- 监控同步延迟，超过 3 秒告警
- 定期数据对账任务

---

### R7: 团队对新技术栈不熟悉

**风险描述**: 团队首次使用 Seata、Elasticsearch、Canal，学习成本高

**应对措施**:
- 第 1 周组织技术培训（Seata、ES、Canal）
- 架构师编写最佳实践文档
- Pair Programming，老带新

---

## 风险监控与报告机制

### 每周风险评审会议
- **时间**: 每周五下午 15:00-16:00
- **参与人**: 架构师、Tech Lead、PM、测试负责人
- **议程**:
  1. 回顾上周风险状态变化
  2. 识别新增风险
  3. 评估应对措施有效性
  4. 制定下周行动计划

### 风险仪表盘

| 风险 ID | 状态 | 趋势 | 最后更新 |
|---------|------|------|---------|
| R1 | 🟡 进行中 | ↓ 降低 | 2025-12-24 |
| R2 | 🟡 进行中 | → 稳定 | 2025-12-24 |
| R3 | 🟡 进行中 | ↑ 上升 | 2025-12-24 |
| R10 | 🔴 待开始 | - | 2025-12-24 |

**状态说明**:
- 🔴 待开始: 尚未采取应对措施
- 🟡 进行中: 应对措施执行中
- 🟢 已关闭: 风险已解决或接受

---

## 风险应对总预算

| 类别 | 预算 (人天) | 说明 |
|-----|------------|------|
| Seata 性能优化 | 10 | 集群部署 + TCC 改造 |
| ES 集群优化 | 15 | 集群规划 + 索引优化 |
| Redis 高可用 | 8 | 哨兵模式 + 缓存策略 |
| Nacos 集群部署 | 5 | 3 节点集群 |
| 技术培训 | 20 | 团队培训 + 文档编写 |
| **总计** | **58** | 约 12 人周 |

---

## 附录：风险评估方法论

### 概率等级定义
- **高 (70%-90%)**: 几乎肯定会发生
- **中 (30%-70%)**: 有可能发生
- **低 (< 30%)**: 不太可能发生

### 影响等级定义
- **高**: 项目失败或严重延期（> 4 周）
- **中**: 项目部分目标无法达成或中度延期（1-4 周）
- **低**: 对项目影响轻微（< 1 周）

---

**文档维护**: PMO + 架构组
**审阅人**: CTO
**下次评审**: 2025-01-07（每两周一次）
