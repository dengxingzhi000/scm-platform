apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-inventory
  namespace: scm-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-inventory
  template:
    metadata:
      labels:
        app: scm-inventory
    spec:
      containers:
        - name: scm-inventory
          image: docker.io/scmcloud/scm-inventory:latest
          ports:
            - containerPort: 8202
          envFrom:
            - configMapRef:
                name: scm-config
            - secretRef:
                name: scm-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8202
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8202
            initialDelaySeconds: 20
            periodSeconds: 5
