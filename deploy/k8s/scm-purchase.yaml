apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-purchase
  namespace: scm-platform
  labels:
    app: scm-purchase
    tier: business
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-purchase
  template:
    metadata:
      labels:
        app: scm-purchase
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8207"
    spec:
      containers:
        - name: scm-purchase
          image: scm-platform/scm-purchase:latest
          ports:
            - containerPort: 8207
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: NACOS_SERVER_ADDR
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: nacos-server
            - name: NACOS_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: nacos-namespace
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: db-host
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: db-password
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: redis-host
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: redis-password
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8207
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8207
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: scm-purchase
  namespace: scm-platform
spec:
  selector:
    app: scm-purchase
  ports:
    - port: 8207
      targetPort: 8207
      name: http
  type: ClusterIP
