apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-warehouse
  namespace: scm-platform
  labels:
    app: scm-warehouse
    tier: business
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-warehouse
  template:
    metadata:
      labels:
        app: scm-warehouse
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8204"
    spec:
      containers:
        - name: scm-warehouse
          image: scm-platform/scm-warehouse:latest
          ports:
            - containerPort: 8204
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: NACOS_SERVER_ADDR
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: nacos-server
            - name: NACOS_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: nacos-namespace
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: db-host
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: db-password
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: redis-host
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: redis-password
            - name: JAVA_OPTS
              value: "-Xms512m -Xmx1g -XX:+UseG1GC"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8204
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8204
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: scm-warehouse
  namespace: scm-platform
spec:
  selector:
    app: scm-warehouse
  ports:
    - port: 8204
      targetPort: 8204
      name: http
  type: ClusterIP
