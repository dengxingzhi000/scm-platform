apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-warehouse
  namespace: scm-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-warehouse
  template:
    metadata:
      labels:
        app: scm-warehouse
    spec:
      containers:
        - name: scm-warehouse
          image: docker.io/scmcloud/scm-warehouse:latest
          ports:
            - containerPort: 8204
          envFrom:
            - configMapRef:
                name: scm-config
            - secretRef:
                name: scm-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8204
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8204
            initialDelaySeconds: 20
            periodSeconds: 5
