apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-logistics
  namespace: scm-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-logistics
  template:
    metadata:
      labels:
        app: scm-logistics
    spec:
      containers:
        - name: scm-logistics
          image: docker.io/scmcloud/scm-logistics:latest
          ports:
            - containerPort: 8205
          envFrom:
            - configMapRef:
                name: scm-config
            - secretRef:
                name: scm-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8205
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8205
            initialDelaySeconds: 20
            periodSeconds: 5
