apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-logistics
  namespace: scm-platform
  labels:
    app: scm-logistics
    tier: business
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-logistics
  template:
    metadata:
      labels:
        app: scm-logistics
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8205"
    spec:
      containers:
        - name: scm-logistics
          image: scm-platform/scm-logistics:latest
          ports:
            - containerPort: 8205
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: NACOS_SERVER_ADDR
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: nacos-server
            - name: NACOS_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: nacos-namespace
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: db-host
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: db-password
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: scm-config
                  key: redis-host
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: scm-secrets
                  key: redis-password
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8205
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8205
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: scm-logistics
  namespace: scm-platform
spec:
  selector:
    app: scm-logistics
  ports:
    - port: 8205
      targetPort: 8205
      name: http
  type: ClusterIP
