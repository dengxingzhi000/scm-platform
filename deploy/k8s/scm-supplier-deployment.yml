apiVersion: apps/v1
kind: Deployment
metadata:
  name: scm-supplier
  namespace: scm-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scm-supplier
  template:
    metadata:
      labels:
        app: scm-supplier
    spec:
      containers:
        - name: scm-supplier
          image: docker.io/scmcloud/scm-supplier:latest
          ports:
            - containerPort: 8206
          envFrom:
            - configMapRef:
                name: scm-config
            - secretRef:
                name: scm-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8206
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8206
            initialDelaySeconds: 20
            periodSeconds: 5
