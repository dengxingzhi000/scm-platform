# Seata 注册与配置中心配置

registry {
  # 使用 Nacos 作为注册中心
  type = "nacos"

  nacos {
    application = "seata-server"
    serverAddr = "nacos:8848"
    group = "SEATA_GROUP"
    namespace = ""
    cluster = "default"
    username = "nacos"
    password = "nacos" # 开发默认值，生产环境务必修改
  }
}

config {
  # 使用 Nacos 作为配置中心
  type = "nacos"

  nacos {
    serverAddr = "nacos:8848"
    namespace = ""
    group = "SEATA_GROUP"
    username = "nacos"
    password = "nacos" # 开发默认值，生产环境务必修改
    dataId = "seataServer.properties"
  }
}